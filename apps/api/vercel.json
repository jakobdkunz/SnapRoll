{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm -w -s install --frozen-lockfile && pnpm dlx prisma migrate deploy --schema=../../prisma/schema.prisma && pnpm dlx prisma generate --schema=../../prisma/schema.prisma && pnpm -w -s build:api"
}
