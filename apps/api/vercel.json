{
  "buildCommand": "cd ../.. && npm install --include=dev && cd apps/api && npm install --include=dev && npx prisma generate --schema=../../prisma/schema.prisma && npm run build",
  "installCommand": "cd ../.. && npm install --include=dev",
  "framework": "nextjs"
}
